#include "../tree.h"
#include "../expand.h"

/* expand an arithmetic expression
 */
union node*
expand_arith(struct nargarith* arith, union node** nptr, int flags) {
  union node* n = *nptr;

  return n;
}

